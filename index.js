  
let equation = "x * y = 62";

let randomInteger = Math.floor(Math.random() * Number.MAX_SAFE_INTEGER) + 1;

let equation = "(n % m) === 4";

let set = [10, 5, 20, 15];
let maxNumber = set.reduce((max, current) => {
  return Math.max(max, current);
}, 0);
